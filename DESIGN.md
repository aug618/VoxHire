# 面试 Agent 设计方案 (v0.3) — 已通过 brainstorming 完成决策

## 一、项目愿景

构建一个 AI 面试官，支持实时语音+虚拟形象交互，用户提供面试范围与辅助资料后，LLM 分析并主导面试，事后给出评估报告。

核心原则：
- **本地优先**：开源免费模型兜底，隐私数据不上云
- **云端增强**：兼容大模型 API，追求更好效果时可切换
- **模块化**：各组件可独立替换

---

## 二、核心功能模块

### 2.1 面试准备
- 用户输入面试范围（岗位、技术栈、难度等）
- 上传辅助资料（简历、JD、项目文档等）
- 选择面试风格（技术深度、行为面试、系统设计、混合等）
- 选择模型配置（本地 / API / 混合）

### 2.2 实时面试
- 虚拟形象 + 语音对话界面
- 实时语音识别（STT）
- AI 面试官语音输出（TTS）+ 虚拟形象
- 根据回答动态调整问题深度/方向
- 追问、打断、引导等面试行为

### 2.3 面试分析
- 面试结束后生成评估报告
- 维度：技术准确度、沟通表达、思维逻辑、知识广度等
- 逐题点评 + 综合评分
- 改进建议

---

## 三、技术架构（初步设想）

```
┌─────────────────────────────────────────────────────┐
│                    前端 (Web UI)                      │
│  React/Next.js + MediaStream API（音频）            │
├─────────────────────────────────────────────────────┤
│                   后端 (Python)                       │
│  FastAPI + WebSocket + 会话管理                       │
├──────────┬──────────┬──────────┬────────────────────┤
│  STT     │  LLM     │  TTS     │  虚拟形象            │
│  Whisper │  Ollama  │  Edge-TTS│  (前端渲染)          │
│  (本地)  │  /API    │  /Coqui  │                     │
└──────────┴──────────┴──────────┴────────────────────┘
```

### 3.1 各层说明

| 层级 | 本地方案 | 云端方案 |
|------|---------|---------|
| STT | Whisper (faster-whisper) | Deepgram / Azure Speech |
| LLM | Ollama (Qwen/Llama/DeepSeek) | OpenAI / Claude / Gemini |
| TTS | Edge-TTS / Coqui TTS / Piper | ElevenLabs / OpenAI TTS |

### 3.2 关键问题（已决策）

1. **交互形态** → 语音 + 虚拟形象（MVP 不含真人视频流）
2. **STT 实时性** → 先句子级识别，架构预留流式接口
3. **LLM 面试策略** → 先单 Agent Prompt 驱动，架构预留多 Agent 接口
4. **部署方式** → Web 应用（本地服务 + 浏览器），后续可加 Electron 壳
5. **面试上下文管理** → 混合策略：固定头 + 阶段摘要 + 滑动窗口
6. **对话管道** → LLM 流式输出 + TTS 分段合成，边生成边播放，首次响应 < 5 秒

---

## 四、LLM 面试策略设计

### 4.1 面试流程状态机

```
[初始化] → [开场白] → [核心问答循环] → [结束语] → [生成报告]
                ↑          ↓
                └─追问/跳题─┘
```

### 4.2 Prompt 结构（初步）

```
System: 你是一个{风格}的面试官，负责{岗位}面试。
面试范围：{范围}
候选人背景：{简历摘要}
规则：
- 根据回答质量动态调整难度
- 答对深挖，答错引导
- 每题追问不超过{n}次
- 总时长控制在{m}分钟
```

### 4.3 多模型混合策略（可选）

- 出题/追问 → 强模型（API）
- 语音转文字 → 本地模型
- 报告生成 → 强模型（API）
- 简单寒暄 → 本地模型

---

## 五、已决策记录

> 所有决策已在 [2026-06-22 brainstorming spec](docs/superpowers/specs/2026-06-22-ai-interviewer-design.md) 中最终确认。以下为摘要：

1. **交互形态** → 语音 + 虚拟形象
2. **面试类型** → 技术面 + 行为面为主，同时支持综合面
3. **多轮对话记忆** → 混合策略（固定头 + 阶段摘要 + 滑动窗口）
4. **评估标准** → 7 维（4 技术 + 3 行为），5 级评分 + 行为锚定描述
5. **用户画像** → 个人练习优先，架构预留企业场景
6. **技术栈偏好** → React/Next.js + Python/FastAPI
7. **MVP 范围** → 详见 spec 文档
8. **对话管道** → LLM 流式输出 + TTS 分段合成（非串行等待），首次响应延迟 < 5 秒

---

## 六、参考文档

完整的系统设计 spec（含 6 个核心模块设计、技术选型、非功能需求等）见：
[docs/superpowers/specs/2026-06-22-ai-interviewer-design.md](docs/superpowers/specs/2026-06-22-ai-interviewer-design.md)

实现计划（含 16 个任务，全部同步流式管道设计）见：
[docs/superpowers/plans/2026-06-22-ai-interviewer.md](docs/superpowers/plans/2026-06-22-ai-interviewer.md)

---

*本文档为设计讨论稿，最终决策以 spec 文档为准。后续重大变更先更新 spec。*
# 面试 Agent 设计方案 (v0.2) — 已通过 brainstorming 完成决策

## 一、项目愿景

构建一个 AI 面试官，支持实时语音+虚拟形象交互，用户提供面试范围与辅助资料后，LLM 分析并主导面试，事后给出评估报告。

核心原则：
- **本地优先**：开源免费模型兜底，隐私数据不上云
- **云端增强**：兼容大模型 API，追求更好效果时可切换
- **模块化**：各组件可独立替换

---

## 二、核心功能模块

### 2.1 面试准备
- 用户输入面试范围（岗位、技术栈、难度等）
- 上传辅助资料（简历、JD、项目文档等）
- 选择面试风格（技术深度、行为面试、系统设计、混合等）
- 选择模型配置（本地 / API / 混合）

### 2.2 实时面试
- 虚拟形象 + 语音对话界面
- 实时语音识别（STT）
- AI 面试官语音输出（TTS）+ 虚拟形象
- 根据回答动态调整问题深度/方向
- 追问、打断、引导等面试行为

### 2.3 面试分析
- 面试结束后生成评估报告
- 维度：技术准确度、沟通表达、思维逻辑、知识广度等
- 逐题点评 + 综合评分
- 改进建议

---

## 三、技术架构（初步设想）

```
┌─────────────────────────────────────────────────────┐
│                    前端 (Web UI)                      │
│  React/Next.js + MediaStream API（音频）            │
├─────────────────────────────────────────────────────┤
│                   后端 (Python)                       │
│  FastAPI + WebSocket + 会话管理                       │
├──────────┬──────────┬──────────┬────────────────────┤
│  STT     │  LLM     │  TTS     │  虚拟形象            │
│  Whisper │  Ollama  │  Edge-TTS│  (前端渲染)          │
│  (本地)  │  /API    │  /Coqui  │                     │
└──────────┴──────────┴──────────┴────────────────────┘
```

### 3.1 各层说明

| 层级 | 本地方案 | 云端方案 |
|------|---------|---------|
| STT | Whisper (faster-whisper) | Deepgram / Azure Speech |
| LLM | Ollama (Qwen/Llama/DeepSeek) | OpenAI / Claude / Gemini |
| TTS | Edge-TTS / Coqui TTS / Piper | ElevenLabs / OpenAI TTS |

### 3.2 关键问题（已决策）

1. **交互形态** → 语音 + 虚拟形象（MVP 不含真人视频流）
2. **STT 实时性** → 先句子级识别，架构预留流式接口
3. **LLM 面试策略** → 先单 Agent Prompt 驱动，架构预留多 Agent 接口
4. **部署方式** → Web 应用（本地服务 + 浏览器），后续可加 Electron 壳
5. **面试上下文管理** → 混合策略：固定头 + 阶段摘要 + 滑动窗口

---

## 四、LLM 面试策略设计

### 4.1 面试流程状态机

```
[初始化] → [开场白] → [核心问答循环] → [结束语] → [生成报告]
                ↑          ↓
                └─追问/跳题─┘
```

### 4.2 Prompt 结构（初步）

```
System: 你是一个{风格}的面试官，负责{岗位}面试。
面试范围：{范围}
候选人背景：{简历摘要}
规则：
- 根据回答质量动态调整难度
- 答对深挖，答错引导
- 每题追问不超过{n}次
- 总时长控制在{m}分钟
```

### 4.3 多模型混合策略（可选）

- 出题/追问 → 强模型（API）
- 语音转文字 → 本地模型
- 报告生成 → 强模型（API）
- 简单寒暄 → 本地模型

---

\#\#\ 五、已决策记录\n\n>\ 所有决策已在\ \[2026-06-22\ brainstorming\ spec]\(\.\./docs/superpowers/specs/2026-06-22-ai-interviewer-design\.md\)\ 中最终确认。以下为摘要：\n\n1\.\ \*\*交互形态\*\*\ →\ 语音\ \+\ 虚拟形象\n2\.\ \*\*面试类型\*\*\ →\ 技术面\ \+\ 行为面为主，同时支持综合面\n3\.\ \*\*多轮对话记忆\*\*\ →\ 混合策略（固定头\ \+\ 阶段摘要\ \+\ 滑动窗口）\n4\.\ \*\*评估标准\*\*\ →\ 7\ 维（4\ 技术\ \+\ 3\ 行为），5\ 级评分\ \+\ 行为锚定描述\n5\.\ \*\*用户画像\*\*\ →\ 个人练习优先，架构预留企业场景\n6\.\ \*\*技术栈偏好\*\*\ →\ React/Next\.js\ \+\ Python/FastAPI\n7\.\ \*\*MVP\ 范围\*\*\ →\ 详见\ spec\ 文档\n\n---\n\n\#\#\ 六、参考文档\n\n完整的系统设计\ spec（含\ 6\ 个核心模块设计、技术选型、非功能需求等）见：\n\[docs/superpowers/specs/2026-06-22-ai-interviewer-design\.md]\(docs/superpowers/specs/2026-06-22-ai-interviewer-design\.md\)\n\n---\n\n\*本文档为设计讨论稿，最终决策以\ spec\ 文档为准。后续重大变更先更新\ spec。\*
## 五、已决策记录

> 所有决策已在 [2026-06-22 brainstorming spec](../docs/superpowers/specs/2026-06-22-ai-interviewer-design.md) 中最终确认。以下为摘要：

1. **交互形态** → 语音 + 虚拟形象
2. **面试类型** → 技术面 + 行为面为主，同时支持综合面
3. **多轮对话记忆** → 混合策略（固定头 + 阶段摘要 + 滑动窗口）
4. **评估标准** → 7 维（4 技术 + 3 行为），5 级评分 + 行为锚定描述
5. **用户画像** → 个人练习优先，架构预留企业场景
6. **技术栈偏好** → React/Next.js + Python/FastAPI
7. **MVP 范围** → 详见 spec 文档

---

## 六、参考文档

完整的系统设计 spec（含 6 个核心模块设计、技术选型、非功能需求等）见：
[docs/superpowers/specs/2026-06-22-ai-interviewer-design.md](docs/superpowers/specs/2026-06-22-ai-interviewer-design.md)

---

*本文档为设计讨论稿，最终决策以 spec 文档为准。后续重大变更先更新 spec。*
