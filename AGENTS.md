# 仓库指南

## 项目背景与资料

本仓库用于开发 VoxHire（AI 语音面试官）比赛项目。根目录的 `报名帖子.md` 是项目的初步创意，说明目标用户、问题和产品价值；`初赛要求.md` 是比赛规则与交付要求。开发决策应与两份文档保持一致，优先完成可交互、可体验的核心 Demo，而非过早追求完整产品。

与项目维护者的交流、Issue、提交说明和界面文案默认使用中文；只有在维护者特别说明、第三方 API 或代码生态惯例要求时使用英文。

## 项目结构

当前仓库以项目资料为主，尚未初始化应用代码。开始实现后，建议采用以下结构：

- `src/`：应用源代码，按功能拆分模块，例如 `src/features/mock_interview/`。
- `tests/`：自动化测试；也可将测试与被测模块相邻放置。
- `assets/` 或 `public/`：静态图片、音频和演示素材。
- 根目录 Markdown：产品说明、比赛交付记录和开发文档。

不要将构建产物、本地录音、评估报告或密钥提交到仓库。

## 开发、构建与测试

目前尚无已提交的 Python 环境、构建脚本或测试框架。初始化项目后，在 `README.md` 中记录唯一的安装、运行、检查和测试命令，例如 `python -m pytest`。提交前运行格式化、静态检查和相关测试；新增缺陷修复时，应补充回归测试。

优先覆盖核心流程：发起模拟面试、采集回答、生成评估、查看七维报告。测试名称描述可观察行为，例如 `test_generates_report_for_completed_session`。

## 代码风格与命名

遵循所选工具链的格式化与检查规则，避免引入相互冲突的配置。Python 使用 4 空格缩进，模块与函数采用 `snake_case`，类采用 `PascalCase`，常量采用 `UPPER_SNAKE_CASE`。功能目录使用小写下划线，例如 `src/features/mock_interview/`。保持用户可见中文文案简洁，并统一使用产品名 “VoxHire”。

## 提交与合并请求

现有 Git 历史使用简短祈使式提交（`init`）。后续保持单一目的，推荐使用 `feat: 添加语音面试流程`、`fix: 修复报告评分显示` 或 `docs: 补充提交清单`。

合并请求应说明用户可见改动、关联的需求或规则、已执行的验证，并为界面改动附截图或录屏。比赛提交还需保留至少 3 个 TRAE Session ID 和关键开发截图，并在提交前确认体验链接或打包 HTML 可正常交互。
